-- (c) eradicator a.k.a lossycrypt, 2017-2021, not seperately licensable
-- -------------------------------------------------------------------------- --

local const = {}

const.index = {
  style_data = {
    slider_value_tooltip_postfix = 1,
    }
  }
  
const.path = {
  -- LuaGuiElement.tags root keys must be strings.
  style_data = {'erlib', 'gui-auto_styler'}
  }

return const

