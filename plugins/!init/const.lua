-- (c) eradicator a.k.a lossycrypt, 2017-2021, not seperately licensable
-- -------------------------------------------------------------------------- --

local const = {}

const.name = {
  setting = {
    enabled_plugins = 'erlib:enabled-plugins',
    }
  }

return const